import org.junit.Test;

public class RoleTest {
    @Test
    public void testRole() {
/*        Date startdate = null;
        Date enddate = null;
        SimpleDateFormat formatter = new SimpleDateFormat("yyyy-MM-dd HH:mm");
        try {
            startdate = formatter.parse("2015-08-18 11:00");
            enddate = formatter.parse("2015-08-18 13:00");
        } catch (ParseException e) {
            System.out.println("Parse Exception");
        }

        DataDaoIF<TagEntity, Long> dataDao = new DataItemHibernateDao<>(TagEntity.class);
        List<TagEntity> hours = dataDao.getDataBetweenDates(startdate, enddate);
        for (TagEntity t : hours) {
            System.out.println(t.getColumnName());
            List<HourEntity> hs = t.getHourEntities();
            for (HourEntity h : hs) {
                System.out.println(h.getDtime() + ":::" + h.getValue());
            }
        }*/
    }
}

